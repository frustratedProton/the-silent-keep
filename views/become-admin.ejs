<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Become an Admin</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('partials/header', { user }) %>

    <main class="main-content">
        <section id="become-admin" class="form-section">
            <h1 class="form-title">Want to Become an Admin?</h1>
            <form action="/become-admin" method="POST" class="form-container">
                <div class="form-group">
                    <label for="passcode" class="form-label">Enter Passcode</label>
                    <input
                        id="passcode"
                        name="passcode"
                        type="password"
                        required
                        placeholder="Enter the secret passcode"
                        class="form-input"
                    />
                </div>
                <button type="submit" class="form-button">Submit</button>
            </form>
        </section>
    </main>

    <%- include('partials/footer') %>
</body>
</html>
